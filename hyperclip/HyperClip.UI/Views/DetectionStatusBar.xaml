<UserControl x:Class="HyperClip.UI.Views.DetectionStatusBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource GlassSurfaceBrush}" CornerRadius="6" Padding="8,6"
            BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="1" Cursor="Hand">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <Ellipse Width="6" Height="6" Margin="0,0,6,0" VerticalAlignment="Center">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="{StaticResource TextTertiaryBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPolling}" Value="True">
                                    <Setter Property="Fill" Value="{StaticResource SuccessBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="Innertube" FontSize="10" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                <TextBlock Text=" | " FontSize="10" Foreground="{StaticResource TextTertiaryBrush}"/>
                <TextBlock FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"
                           FontFamily="{StaticResource MonoFont}" VerticalAlignment="Center">
                    <Run Text="{Binding ActiveSessions, FallbackValue='0'}"/>
                    <Run Text="/"/>
                    <Run Text="{Binding TotalSessions, FallbackValue='30'}"/>
                    <Run Text=" sessions"/>
                </TextBlock>
            </StackPanel>
            <Grid Margin="0,0,0,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusText}" FontSize="9"
                           Foreground="{StaticResource TextTertiaryBrush}"/>
                <TextBlock Grid.Column="1" Text="{Binding ChannelCount, StringFormat='{}{0} ch'}"
                           FontSize="9" Foreground="{StaticResource TextTertiaryBrush}"
                           FontFamily="{StaticResource MonoFont}"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
